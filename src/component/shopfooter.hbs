<div class="row">    
    {{#each footer}}
        <span class="col-2 nav">
            {{#each this}}
                <a class="nav-item nav-link {{#if @last}} disabled {{/if}}" href="{{this.link}}"><small>{{this.text}}</small></a>
            {{/each}}
        </span>
    {{/each}} 
</div>