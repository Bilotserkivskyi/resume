<div>
    {{#with main}}
        <main>
            <section>
                {{>title}}
                <ul>
                    {{#each education}}
                    <li>
                        {{name}}
                        {{#unless isEnd}} (ще в процесі) {{/unless}}
                    </li>
                    {{/each}}                
                </ul>
            </section>

            <section>
                {{>title certificat}}
                <ul>
                    {{#each certificates}}
                    <li>
                        {{name}} ({{id}})
                    </li>
                    {{/each}}                
                </ul>
            </section>
        </main> 
    {{/with}}  
</div>